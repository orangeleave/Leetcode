class Solution {
    // All numbers have even number of factors except square number(e.g: factor of 9:1,3,9),
    // which means square number must turn on because of odd number of factors,
    // so we only need to compute the number of square number less equal than n.
    public int bulbSwitch(int n) {
        return (int)Math.sqrt(n);
    }
}
